import { ReservationProperties } from './types';

export const reservationProperties: Record<ReservationProperties, ReservationProperties> = {
  dateTo: 'dateTo',
  dateFrom: 'dateFrom',
};

export const reservationPageAlertMessages = {
  openModalAlert: '날짜를 먼저 선택해 주세요',
}